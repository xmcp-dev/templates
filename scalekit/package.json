{
  "name": "xmcp-scalekit",
  "version": "0.1.0",
  "private": true,
  "description": "xmcp server with Scalekit OAuth for per-user team access (share one URL, individual sign-in, private data + optional RBAC)",
  "keywords": ["scalekit", "authentication", "oauth", "per-user", "rbac", "mcp", "team"],
  "scripts": {
    "build": "xmcp build",
    "dev": "xmcp dev",
    "start": "node dist/http.js"
  },
  "dependencies": {
    "@xmcp-dev/scalekit": "latest",
    "zod": "^4.0.10"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.3",
    "xmcp": "latest"
  }
}
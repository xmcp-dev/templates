{
  "name": "@xmcp-templates/typescript",
  "version": "0.1.0",
  "private": true,
  "description": "An xmcp application with tool syntax",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "xmcp build",
    "dev": "xmcp dev",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .xmcp"
  },
  "dependencies": {
    "@xmcp-templates/catalog": "workspace:*",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "eslint": "^9.23.0",
    "typescript": "^5.7.0",
    "xmcp": "0.4.0"
  }
}

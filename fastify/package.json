{
  "name": "xmcp-fastify",
  "version": "0.1.0",
  "private": true,
  "description": "A Fastify server with xmcp adapter integration",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "xmcp build && tsc",
    "dev": "xmcp dev & tsx watch src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "fastify": "^5.0.0",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "@types/node": "^22.19.2",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "xmcp": "latest"
  }
}
